Generate a JSON summary of this repository. The output must be valid JSON with the following fields:

- `name`: The project name
- `description`: A one-sentence description of what the project does
- `language`: The primary programming language
- `dependencies`: An array of key dependency names

Respond with ONLY the JSON object, no markdown code fences or additional text.
